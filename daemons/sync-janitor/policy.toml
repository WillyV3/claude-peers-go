[policy]
name = "sync-janitor"

[policy.constraints]
max_file_deletes = 0
max_file_renames = 0
allowed_paths = [
    "~/projects/",
    "~/hfl-projects/"
]
forbidden_paths = [
    "~/.ssh/",
    "~/.config/",
    "~/.claude/"
]
